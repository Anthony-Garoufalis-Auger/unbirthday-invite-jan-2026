# Iteration 3 patch (routes + copy wiring)

This patch assumes **Next.js App Router** (an `app/` directory).
Copy these files into your repo root and **overwrite** existing ones where paths overlap.

If your repo uses the Pages Router (`pages/`), don’t apply this patch as-is.
